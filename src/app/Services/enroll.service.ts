export class EnrollService{

  OnEnrollClick(title: string){
    alert('Thank you for enrolling to ' + title + ` course.`)
  }
  //We are reusing this method in both components
}
//Service class is a simple TS class witout any decorator
